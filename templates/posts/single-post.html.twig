{% extends'base.html.twig' %}

{% block title %}{{post.title}}{% endblock %}

{% block body %}
    <div class="container-fluid">
    <h2>{{ post.title }}</h2>
    <img src="{{ vich_uploader_asset(post, 'thumbnailFile') }}" alt="banner">
    {{ post.content |raw }}
    </div>
{#    todo facebook#}
{% endblock %}